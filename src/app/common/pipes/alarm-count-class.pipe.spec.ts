import { AlarmCountClassPipe } from './alarm-count-class.pipe';

describe('AlarmCountClassPipe', () => {
  it('create an instance', () => {
    const pipe = new AlarmCountClassPipe();
    expect(pipe).toBeTruthy();
  });
});
