import { NgForObjectPipe } from './ng-for-object.pipe';

describe('NgForObjectPipe', () => {
  it('create an instance', () => {
    const pipe = new NgForObjectPipe();
    expect(pipe).toBeTruthy();
  });
});
